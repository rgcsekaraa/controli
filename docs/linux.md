# Linux

Linux can host and join sessions.

Host requirements:

- A normal shell such as `bash`, `zsh`, or `sh`.
- PTY support from the operating system.
- `tmux` for persistent hosted shells.
- Outbound HTTPS and WebSocket access to the relay.

Ubuntu uses the `linux-amd64` binary on common desktop and server installs.
