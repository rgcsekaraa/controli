# Troubleshooting

If a guest cannot join:

- Confirm the invite code has not expired.
- Confirm the relay URL is configured on the host.
- Confirm both machines can make outbound WebSocket connections.
- Restart the host share to create a new invite.

If rendering is slow, use the default browser terminal instead of `--console`.

