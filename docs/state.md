# State

Controli reads local configuration from:

```text
~/.controli/state.json
```

The state file stores relay configuration and workspaces. Invite secrets are generated for each share session and are not reused.

