# Join

The guest joins with:

```bash
controli join 1234567
```

The command opens a local browser terminal and keeps a small local HTTP server alive for the duration of the session.

Direct console rendering is available for diagnostics:

```bash
controli join 1234567 --console
```

