# Security

Treat every invite code like a password while it is valid.

The guest controls the hosted shell for the lifetime of the session. Use a dedicated workspace and avoid sharing more access than needed.

Run a relay you control. Rotate invite codes by stopping and starting the host share.

