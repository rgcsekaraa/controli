# Install

Use the binary that matches the target machine.

For Windows, download `controli-windows-amd64.exe`.

For common Ubuntu systems, download `controli-linux-amd64`.

For Apple Silicon Macs, download `controli-darwin-arm64`.

After downloading on Unix systems, mark the file executable with `chmod +x`.

